version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - "11059:8000"
    environment:
      - DATABASE_URL=postgresql://admin:password@postgres:5432/app059
      - REDIS_URL=redis://redis:6379

  frontend:
    build: ./frontend
    ports:
      - "12059:3000"
    environment:
      - REACT_APP_API_URL=http://localhost:11059

  postgres:
    image: postgres:15-alpine
    environment:
      POSTGRES_DB: app059
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: password
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine

volumes:
  postgres_data:
