version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - "11021:8000"
    environment:
      - DATABASE_URL=postgresql://admin:password@postgres:5432/app021
      - REDIS_URL=redis://redis:6379

  frontend:
    build: ./frontend
    ports:
      - "12021:3000"
    environment:
      - REACT_APP_API_URL=http://localhost:11021

  postgres:
    image: postgres:15-alpine
    environment:
      POSTGRES_DB: app021
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: password
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine

volumes:
  postgres_data:
