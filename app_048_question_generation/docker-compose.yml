version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - "11048:8000"
    environment:
      - DATABASE_URL=postgresql://admin:password@postgres:5432/app048
      - REDIS_URL=redis://redis:6379

  frontend:
    build: ./frontend
    ports:
      - "12048:3000"
    environment:
      - REACT_APP_API_URL=http://localhost:11048

  postgres:
    image: postgres:15-alpine
    environment:
      POSTGRES_DB: app048
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: password
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine

volumes:
  postgres_data:
