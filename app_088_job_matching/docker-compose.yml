version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - "11088:8000"
    environment:
      - DATABASE_URL=postgresql://admin:password@postgres:5432/app088
      - REDIS_URL=redis://redis:6379

  frontend:
    build: ./frontend
    ports:
      - "12088:3000"
    environment:
      - REACT_APP_API_URL=http://localhost:11088

  postgres:
    image: postgres:15-alpine
    environment:
      POSTGRES_DB: app088
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: password
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine

volumes:
  postgres_data:
